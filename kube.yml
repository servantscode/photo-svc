apiVersion: apps/v1
kind: Deployment
metadata:
  name: photo-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: photo-svc
  template:
    metadata:
      labels:
        app: photo-svc
    spec:
      containers:
      - name: photo-svc
        image: servantscode/photo-svc
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "200Mi"
        env:
        - name: ES_JAVA_OPTS 
          value: "-Xmx128m -Xms128m"
---
kind: Service
apiVersion: v1
metadata:
  name: photo-svc
spec:
  selector:
    app: photo-svc
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
